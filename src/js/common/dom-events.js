/**
 * @fileoverview File to group all the DOM events.
 */

/**
 * List of events associated with DOM interactions
 * @enum {string}
 */
const DOMEvents = {
  CHANGE: 'change',
  CLICK: 'click',
  DOM_CONTENT_LOADED: 'DOMContentLoaded',
  KEY_DOWN: 'keydown',
  KEY_UP: 'keyup',
  LOAD: 'load',
  TRANSITION_END: 'transitionend',
};

export default DOMEvents;
